# Opcional pero recomendado en Vercel / serverless: sesiones del QR representante
# Sin esto, cada instancia tiene su propia memoria y el enlace falla con "expiró o no es válido".
# Crear base gratis: https://upstash.com → Redis → copiar REST URL y token
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
